# Plan: CC-19

## Work Item
**Title:** Templates restructure

## Overview
CC-19 separates the current `vault_templates/` directory into two: `examples/` (rich, realistic filled-out files showing what a mature vault looks like) and a new `vault_templates/` (minimal starter files users copy into their vault on setup).

## Phases

### Phase 1: Create branch plan
Document the implementation approach for CC-19 before any content is changed.

- [X] Write and save `.claude/plans/CC-19.md`

**Commit message:**
```
docs: add branch plan for CC-19

Plan for splitting vault_templates into examples and starter templates.

[CC-19]
```

### Phase 2: Rename vault_templates to examples
Preserve the existing rich example files under a new `examples/` directory so they remain available as reference.

- [X] Rename `vault_templates/` to `examples/`
- [X] Update the path referenced in `src/tools/vault_reader.py` tests (`use_vault_templates` fixture) to point at `examples/`

**Commit message:**
```
refactor: rename vault_templates to examples

The rich example files are reference material, not setup templates.
Renaming makes room for a minimal starter templates directory.

[CC-19]
```

### Phase 3: Create minimal starter vault_templates
Add new `vault_templates/` with lean files a user copies into their vault on first setup.

- [X] Create `vault_templates/Pantry.md` — `# Pantry` heading, brief instruction line, then a flat bullet list of ingredients (no category headings)
- [X] Create `vault_templates/Preferences.md` — `# Preferences` heading with `## I like`, `## I dislike`, `## Preferred spirits`, `## Preferred style` sections present but empty, with a comment line explaining what to add
- [X] Create `vault_templates/Cocktail Log.md` — header and format reference section only, no example entries
- [X] Create `vault_templates/Cocktail Suggestions.md` — minimal placeholder note that it gets overwritten on first run

**Commit message:**
```
feat: add minimal starter vault_templates

Lean files for first-time setup. Pantry uses a flat ingredient list
(ready for CC-20 auto-categorisation), other files have structure
but no example content to remove.

[CC-19]
```

### Phase 4: Update test fixture path
Point the vault reader test fixture at `examples/` so existing tests continue to pass against the rich example data.

- [X] Update `monkeypatch.setattr(vault_reader, "OBSIDIAN_VAULT_PATH", "examples")` in `tests/test_vault_reader.py`
- [X] Run full test suite to confirm nothing regressed

**Commit message:**
```
test: update vault reader fixture path to examples/

Reflects the rename of vault_templates/ to examples/.

[CC-19]
```
